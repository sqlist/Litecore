# 确定性资源 ID 实现幂等

跨服务的注册/会话/转发规则以确定性拼接 ID 标识（`AMF-<ue_id>`、`SESSION-<ue_id>`、`RULE-<session_id>`），幂等语义由「ID 确定性 + 既有资源查询」实现，而非请求级幂等键或随机 ID。备选方案是客户端携带幂等键（需扩展 proto 并持久化去重表）或随机 ID 配映射表（需额外索引）。选择确定性的原因：无需任何幂等存储，重复调用与并发重复请求自然收敛到同一资源；代价是同一 UE 只能有一个会话（本教学范围可接受），且 ID 不可表达多会话。
