# LiteCore

5G 核心网教学与实验平台的研究语境：以 gRPC 服务接口替代 3GPP 标准接口，研究核心网元间的状态传递、并发处理、错误传播与控制面延迟。本文件只收录本项目特有的领域术语。

## 语言

**UE（用户设备）**:
发起注册/注销的模拟终端；注册时携带信道质量（信号功率与 SINR）。
_Avoid_: 终端、手机、客户端

**信道模型（Channel Model）**:
产生每个 UE 信道质量（信号功率、SINR）的可替换模块；接入门控的阈值不属于它。
_Avoid_: 信道模拟器

**接入门控（Admission Gate）**:
注册准入条件：信号功率不低于 -110 dBm 且 SINR 不低于 0 dB。仅在创建时生效；注册成立后信道退化不撤销注册。
_Avoid_: 校验、准入控制

**注册（Register）**:
UE 在 AMF 建立注册记录并取得会话的过程。幂等：同一 UE 重复注册返回既有会话与 IP，不重新分配。
_Avoid_: 登记、附着（attach）

**注销（Deregister）**:
解除 UE 注册：删除会话与转发规则、回收 IP、清除注册记录。幂等：资源已不存在时仍视为成功。
_Avoid_: 去注册、下线

**会话（Session）**:
SMF 维护的 UE 与 IP 的绑定，处于「创建中」「活动」或「删除中」状态。
_Avoid_: 连接、承载

**转发规则（Forwarding Rule）**:
UPF 中与一个会话对应的模拟转发条目；模拟包计数随其生命周期起止。
_Avoid_: 流表项、PDR

**IP 池（IP Pool）**:
SMF 管理的可分配 IP 集合；注册取得 IP，注销回收 IP。
_Avoid_: 地址池

**接入成功率（Admission Success Rate）**:
注册成功请求数占总请求数的比例。
_Avoid_: 成功率

**状态漂移（State Drift）**:
单个服务重启后，其余服务的记录指向已消失资源的跨服务不一致状态。
_Avoid_: 数据不一致

**补偿（Compensation）**:
跨服务调用失败时，撤销本服务已占用的资源（会话、IP、转发规则）。
_Avoid_: 回滚、清理

**孤儿资源（Orphaned Resource）**:
上游已撤销、下游却残留的资源。本项目不设后台清扫；后续请求会检测参数冲突而不静默绑定，但不保证自动清理。
_Avoid_: 残留状态、泄漏
